{
  "name": "api-trab-final",
  "version": "1.0.0",
  "description": "API REST + GraphQL com JWT, testes (Mocha/Chai/Supertest/Sinon) e pipeline",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node src/index.js",
    "test": "mocha \"test/**/*.spec.js\" --reporter spec --timeout 8000",
    "test:watch": "mocha \"test/**/*.spec.js\" --watch",
    "lint": "echo 'add your linter here'",
    "coverage": "nyc --reporter=lcov --reporter=text-summary mocha \"test/**/*.spec.js\" --reporter spec --timeout 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "chai": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-graphql": "^0.12.0",
    "graphql": "^16.9.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "mocha": "^10.7.0",
    "sinon": "^17.0.1",
    "supertest": "^7.0.0",
    "nyc": "^15.1.0"
  }
}